using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowFlowerIcon : MonoBehaviour
{

    //public Image icon;

    // Start is called before the first frame update
    void Start()
    {
        //icon.enabled = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
